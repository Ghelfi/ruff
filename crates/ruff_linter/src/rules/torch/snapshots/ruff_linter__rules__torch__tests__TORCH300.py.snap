---
source: crates/ruff_linter/src/rules/torch/mod.rs
---
TORCH300 `DataLoader` with `DistributedSampler` should set `drop_last=True` to avoid uneven last batches across ranks
 --> TORCH300.py:8:1
  |
7 | # Errors (should trigger TORCH300)
8 | DataLoader(dataset, sampler=DistributedSampler(dataset))
  | ^^^^^^^^^^
9 | DataLoader(dataset, sampler=DistributedSampler(dataset), batch_size=32)
  |

TORCH300 `DataLoader` with `DistributedSampler` should set `drop_last=True` to avoid uneven last batches across ranks
  --> TORCH300.py:9:1
   |
 7 | # Errors (should trigger TORCH300)
 8 | DataLoader(dataset, sampler=DistributedSampler(dataset))
 9 | DataLoader(dataset, sampler=DistributedSampler(dataset), batch_size=32)
   | ^^^^^^^^^^
10 |
11 | # Should NOT trigger TORCH300
   |
